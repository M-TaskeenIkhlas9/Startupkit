# web (founder app)
Next.js (stays JS/TS). Dashboard, workflow status, approval inbox, provider selection.
Talks to the FastAPI backend; generate a typed client from its OpenAPI schema. TODO(@eng-product).
