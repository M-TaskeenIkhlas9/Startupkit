# core (the 70%)
The reusable engine: orchestration (durable), company_object (event-sourced),
document_intelligence (the AI layer), compliance (regulatory engine), services (shared),
ports_runtime (registry + conformance), ple (variation), tenancy, security, observability.
(Named `core` rather than `platform` to avoid Python's stdlib `platform` module.)
